---
interface Props {
  tags: string[];
  path: (tag: string) => string;
}

const { tags, path } = Astro.props;
---

{
  tags.length > 0 && (
    <nav>
      {tags.map((tag) => (
        <a href={path(tag)}>
          {tag}
        </a>
      ))}
    </nav>
  )
}
