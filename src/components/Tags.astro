---
import Tag from "@components/Tag.astro";

interface Props {
  tags: string[];
  path?: (tag: string) => string;
}

let { tags, path } = Astro.props;
path ??= (tag) => `tags/${tag}`;
---

<nav class="my-2 flex flex-wrap">
  {tags.map((tag) => <Tag tag={tag} path={path} />)}
</nav>
